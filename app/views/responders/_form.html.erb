<%= form_with(model: responder, local: true) do |form| %>
  <% if responder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(responder.errors.count, "error") %> prohibited this responder from being saved:</h2>

      <ul>
      <% responder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div>
    <%= form.hidden_field( :user_id, :value => current_user.id) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
